/**
 * The options for the init command.
 */
export enum InitOptions {
  /**
   * The command empty
   */
  clean = '',

  /**
   * The name of the project for the target folder.
   * This value is used in the Init Service for execute methods;
   */
  projectName = '--name=',
}
